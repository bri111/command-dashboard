'use client'

import React, { useState, useTransition } from 'react';
import Link from 'next/link';

const CgTabSystem = ({ data }) => {

    return (
        <div className="container h-screen pt-8 px-4">
        </div>
    );
}

export default CgTabSystem;